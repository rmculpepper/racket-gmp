#lang info

;; ========================================
;; pkg info

(define version "1.1")
(define collection "gmp")
(define deps '("base"))
(define pkg-authors '(ryanc))

;; ========================================
;; collect info

(define name "gmp")
