#lang info

;; ========================================
;; pkg info

(define version "1.2")
(define collection "gmp")
(define deps '("base"))
(define pkg-authors '(ryanc))

;; ========================================
;; collect info

(define name "gmp")
